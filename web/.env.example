# Frontend config. Copy to web/.env and fill in from your Supabase project
# (Project Settings → API). Only the anon (public) key goes here — never the
# service role key.
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key
